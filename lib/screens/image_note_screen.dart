import 'package:flutter/material.dart';

class ImageNoteScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Image Note'),
      ),
      body: const Center(
        child: Text('Image Note Screen'),
      ),
    );
  }
}
